// // helpers/linkHelper.js
// import { useParams } from 'react-router-dom';

// export const useLanguageLink = () => {
//   const { lang } = useParams();

//   const createLanguageLink = (to) => {
//     if (to.startsWith('/')) {
//       to = to.substring(1);
//     }
//     return `/${lang}/${to}`;
//   };

//   return createLanguageLink;
// };
